/** @type {import('next').NextConfig} */
const nextConfig = {
  // assetPrefix: process.env.NODE_ENV === 'production' ? '/jhuril45/portfolio/' : '',
  output: 'export',
};

export default nextConfig;
